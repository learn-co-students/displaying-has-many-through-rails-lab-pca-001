<h2><%= @patient.name %></h2>

<% @appointments.each do |t| %>
  <%= t.appointment_datetime.strftime("%B %d, %Y at %H:%M") %></br>
<% end %></br>

<% @doctors.each do |d| %>
  <%= link_to d.name, doctor_path(d) %>
<% end %>